export default class vxToolbar {
    constructor(divToolbar) {
        // esta clase se encarga de gestionar los elementos de la paleta arrastrables a variamosgraph
    }

    addMode( label, iconUrl, drapAndDropCreation){
        //npi
    }
}